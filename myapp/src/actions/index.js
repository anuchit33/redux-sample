
export const setName_action = (name) => ({
    type: 'SET_NAME_ACTION', // use in reducer
    name: name
  })

  export const login_action = () => ({
    type: 'LOGIN_ACTION', // use in reducer
  })

  export const logout_action = () => ({
    type: 'LOGOUT_ACTION', // use in reducer
  })